---
title: 4th November RECAP
draft: false
tags:
  - study
  - Math
  - physics
  - compsci
---
# (COMPPROG) 3D arrays and OOP
So in general 3D array consist of three rows of arrays inside a one massive array
to iterate and use values form array use bascially a nested loop

TO SET UP 2D ARRAY
int[][] x = new int[3][4]; -> 3 ROWS 4 COLUMNS

TO SET UP a multidimensional ARRAY
int[][][] threeDArray = new int [depth][rows][columns]; 

when calling for specific element use x[row][value inside the row]

## OOP
Object oriented programming consist of the multiple different methods that are used to state different characteristics
each object has its own space in memory
SO a CLASS IS A RAW BLUEPRINT of the object

Attributes -> Variables inside our class
Behaviour (actions) -> defined as methods inside our class

so to initialize the object
class Farmers{"your code"}
TO create object use command called new
Farmer f1 = new Farmer(); -> all objects have the same blueprint until changed later
like this

f1.name = "Nick" ->>> so basically by using variable name . attribute we can change it to something custom.



# (PHYSICS) 
